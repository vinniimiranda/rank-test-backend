<strong>Hello</strong>

<p>Here's the results for you alert : <b>{{ keyword }}</b></p>

<div class="list">
  {{#each result}}
  <div class="card">
    <p class="title">{{title}}</p class="title">
    <img class="bg" src="{{galleryURL}}" alt="{{title}}">
    <div class="price">
      Price:
      <span class="price-value">$USD: {{price}}</span>
    </div>
    <div style="margin-bottom: 15px;">
      Location: <span class="location"> {{location}}</span>
    </div>
    <a class="link" href="{{viewItemURL}}">See
      on Ebay</a>

  </div>


  {{/each}}
</div>

<style>
  .list {
    display: grid;
    grid-template-columns: repeat(3, 2fr);
    grid-gap: 10px;
  }

  .card {
    width: 350px;
    height: 400px;
    border-radius: 8px;
    box-shadow: 0px 0px 4px #aaa;
    display: flex;
    text-align: center;
    flex-direction: column;
    justify-content: flex-start;
    align-items: center;
  }

  .title {
    font-size: large;
    font-weight: bold;
  }

  .bg {
    width: 130px;
    height: 130px;
  }

  .location {
    font-weight: bold;
  }

  .price {
    margin-bottom: 15px;

    font-size: 20px;
  }

  .price-value {
    font-weight: bold;
  }

  .link {
    color: #fff;
    background-color: #61f;
    text-decoration: none;
    padding: 10px 20px;
    border-radius: 4px;
    box-shadow: 0px 1px 3px #aaa;


  }

  .link:hover {
    transition: all ease-in-out .2s;
    padding: 12px 24px;
    font-weight: bold;
    background-color: #50c;
  }
</style>